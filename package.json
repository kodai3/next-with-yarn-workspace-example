{
  "name": "with-yarn-workspaces",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace ts-app dev",
    "build": "yarn workspace ts-app build",
    "start": "yarn workspace ts-app start",
    "deploy": "yarn workspace ts-app deploy"
  },
  "devDependencies": {
    "vercel": "^21.3.2"
  },
  "license": "MIT"
}
